
namespace Thorium_Shared
{
    /*public static class SharedStatics
    {
        private static Logger logger = default(Logger);
        public static Logger Logger
        {
            get
            {
                if(logger == default(Logger))
                {
                    logger = new Logger();
                    logger.AddListener(new ConsoleLogListener());
                    logger.AddListener(new FileLogListener());
                }
                return logger;
            }
        }
    }*/
}
